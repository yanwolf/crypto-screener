# 接入正式網的流程

跟程式碼放在一起，才不會找不到。啟動時的硬性檢查會擋住缺漏的項目，
但「該做什麼」寫在這裡。

## 先做（在幣安）

1. 建一個**虛擬子帳戶**專門給機器人，只開「合約帳戶」，其他（槓桿、期權、可切換）都不開。
2. 資金轉進子帳戶後，再從現貨劃轉到 **U 本位合約錢包**。程式讀的是合約權益，錢在現貨會看到 0。
3. 在**主帳戶的子帳戶管理**裡建 API 金鑰（不是登入子帳戶去建）：
   - 只勾「允許合約」，不勾現貨、不勾提幣
   - 設 IP 白名單：填「連線設定 → 執行連線診斷」顯示的伺服器出口 IP
4. 合約帳戶：全倉、關閉聯合保證金。持倉模式單向或雙向都可以，程式會自動偵測並適配
   （雙向時每張單帶 positionSide，且只認自己那一側的部位；同一帳戶給其他程式用另一側不會互相干擾）。
5. 記下 Key 與 Secret，等下填 Zeabur。

資金量的參考：每筆風險 0.25% × 停損 6%，500 U 帳戶的名目約 21 U，
BTC 這類最小下單量較大的幣會被跳過（訊息會寫「數量低於最小下單量」）。
想讓大幣參與，放 2,000 U 以上或把風險調到 0.5%。

## 再做（在 Zeabur 環境變數）

| 變數 | 動作 | 說明 |
|---|---|---|
| `BN_KEY` / `BN_SECRET` | 換成正式網子帳戶的 | 模擬網與正式網是兩套系統 |
| `ALLOW_LIVE` | 設為 `1` | 少這個仍是模擬網 |
| `ADMIN_KEY` | 換一組 16 字元以上隨機字串 | 短於 16 會拒絕啟動 |
| `RISK_PCT` | `0.25` | 超過 1 會拒絕啟動 |
| `LEVERAGE` | `3` | 超過 5 會拒絕啟動 |
| `MAX_POSITIONS` | `3` | 先小，穩了再放 |
| `CG_API_KEY`、Telegram token | 順便換新 | 上次外洩事件的餘波 |

## 不用改任何檔案

上線狀態**全部**在 Zeabur 環境變數，資料夾裡沒有任何東西記錄它。
所以之後整包覆蓋更新不會把你退回模擬網。

| 變數 | 值 |
|---|---|
| `EXTRA_ARGS` | `--live` |
| `ALLOW_LIVE` | `1` |

兩個都要，少一個仍是模擬網。這是刻意的雙重保險。

萬一哪天真的被退回模擬網（例如環境變數被誤刪），伺服器啟動時會發現
「上次是正式網、這次是模擬網」，在日誌和 Telegram 大聲警告。

## 部署前

- 確認 Telegram 配對還在（提醒設定頁）。
- 帳本不用動：正式網與模擬網各自一份（`trader.live.json`、`trader.testnet.json`），
  績效永遠不會混在一起。

## 部署後第一天

- 啟動日誌要看到「交易　正式網（真實資金）」與「⚠ 正在對正式網下單」。
- 模擬單頁把 `每日最多開倉` 設 1、`分數門檻` 設 75。
- 第一筆成交後，到幣安 App 確認：部位在、停損／停利／移動停利三張單都在。
- 都對了才逐步放寬。

## 隨時想回模擬網

移除 `ALLOW_LIVE` 重新部署即可。金鑰可以留著，程式不會用它打正式網。

回模擬網時，正式網帳本原封不動。若正式網還有持倉，啟動時會提醒：
那些部位仍在幣安、停損單仍有效，只是模擬網模式不追蹤也不移損；
切回正式網會自動接續。想讓它們被管理，就別在有持倉時切換，
或先手動平倉再切。

## 績效不如預期時怎麼回頭

1. 正式網先把 `每日最多開倉` 設 0 或按「停止」，讓現有部位自然出場。
2. 全部平倉後移除 `ALLOW_LIVE`，回模擬網繼續累積樣本。
3. 想改參數就改，交易會自動歸入新的策略標籤，跟改前分開統計。
4. 模擬網新參數累積 40 筆以上、期望值為正，再重新上線。
   正式網的舊紀錄留在 `trader.live.json`，不會被覆蓋。

## 模擬網與正式網的實質差異

- 滑價：正式網小市值幣的市價停損可能滑 1–2%，-1R 實際會變 -1.3R 到 -1.6R。
- 流動性：第一週把流動性門檻拉高，只做市值前 100。
- 價格：模擬網的價格與現貨常常對不上，正式網不會有這個問題。


## 正式網與模擬網並存

讓正式網跑已驗證的參數、模擬網繼續實驗。做法是**兩個獨立服務**，同一份程式碼：
實體隔離，模擬網那邊怎麼改、怎麼壞，都碰不到正式部位。

### 現有的模擬網不動，新開一個正式網服務

這是最簡單也最安全的路：模擬網服務、它的 Volume、帳本、Telegram 配對全部原封不動，
正式網從零開始，沒有任何遷移。

同一個專案 → Add Service → 同一個 Git repo → 新服務掛自己的 Volume 到 `/app/cache`。

| 變數 | 新的正式網服務 | 現有的模擬網服務 |
|---|---|---|
| `EXTRA_ARGS` | `--live` | 不動 |
| `ALLOW_LIVE` | `1` | 不動 |
| `BN_KEY` / `BN_SECRET` | 正式網子帳戶 | 不動 |
| `CG_API_KEY` | 把現有那組**搬過來** | **移除** |
| `CG_UPSTREAM` | （不設） | `https://正式網服務網址` |
| `PREFETCH` / `PREFETCH_TTL` | `250` / `72` | `0` |
| `MONITOR_EVERY` | `30` | 不動 |
| `NOTIFY_PREFIX` | `[正式]` | 之後可加 `[模擬]` |
| `ADMIN_KEY` | 新的一組，16 字元以上 | 不動 |
| `TG_TOKEN` | **另建一個 Bot** | 不動 |

**資料層共用、程式不共用。** CoinGecko 金鑰只放正式網一份，模擬網設 `CG_UPSTREAM`
把所有 CoinGecko 請求（含深度資料時間戳）轉給正式網的快取。一份額度、一份快取，
兩套獨立的下單程式與帳本。同一個 IP 開兩組免費金鑰是條款灰色地帶，不要走那條路。

方向是模擬網依賴正式網，不是反過來：正式網上線後很少重新部署，是最穩定的一端；
模擬網是實驗場，正式網偶爾重啟讓它中斷幾十秒無所謂。

模擬網的 CoinGecko 用量會出現在正式網的額度預估裡，兩邊加起來要在 10,000 次以內。

**Telegram 要用第二個 Bot。** 配對機制靠 `getUpdates` 輪詢，Telegram 同一個 token
只允許一個消費者；兩個服務共用一個 Bot，配對時會互搶訊息。
到 BotFather 再建一個，一分鐘的事。正式網 Bot 配到主群，模擬網 Bot 配到另一個靜音群。

### Telegram 怎麼分

兩個服務各自配對。兩種做法：

- **同一個群、靠前綴分**：`[正式]` `[模擬]` 一眼分得出。簡單，但訊息量翻倍。
- **分兩個群**：模擬網配到另一個群並靜音，想看再點進去。推薦這種。

「有正式交易就只通知正式、沒有就通知模擬」這種條件式切換，
我刻意沒做：它會讓你在正式網活躍時看不到模擬網的實驗結果，
而那正是你想比較的東西。分群靜音達到同樣的安靜，又不損失資訊。

### 兩邊可以用不同參數

各自的 `trader.*.json` 獨立，模擬網改參數自動分策略標籤。
模擬網某組參數累積 40 筆以上、期望值明顯優於正式網現行參數時，
再把那組參數搬到正式網——這就是「用模擬網持續追蹤更好策略」的完整流程。
