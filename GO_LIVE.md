# 接入正式網的流程

跟程式碼放在一起，才不會找不到。啟動時的硬性檢查會擋住缺漏的項目，
但「該做什麼」寫在這裡。

## 先做（在幣安）

1. 建一個**虛擬子帳戶**專門給機器人，只開「合約帳戶」，其他（槓桿、期權、可切換）都不開。
2. 資金轉進子帳戶後，再從現貨劃轉到 **U 本位合約錢包**。程式讀的是合約權益，錢在現貨會看到 0。
3. 在**主帳戶的子帳戶管理**裡建 API 金鑰（不是登入子帳戶去建）：
   - 只勾「允許合約」，不勾現貨、不勾提幣
   - 設 IP 白名單：填「連線設定 → 執行連線診斷」顯示的伺服器出口 IP
4. 合約帳戶維持預設：單向持倉、全倉、關閉聯合保證金。程式不支援雙向對沖。
5. 記下 Key 與 Secret，等下填 Zeabur。

資金量的參考：每筆風險 0.25% × 停損 6%，500 U 帳戶的名目約 21 U，
BTC 這類最小下單量較大的幣會被跳過（訊息會寫「數量低於最小下單量」）。
想讓大幣參與，放 2,000 U 以上或把風險調到 0.5%。

## 再做（在 Zeabur 環境變數）

| 變數 | 動作 | 說明 |
|---|---|---|
| `BN_KEY` / `BN_SECRET` | 換成正式網子帳戶的 | 模擬網與正式網是兩套系統 |
| `ALLOW_LIVE` | 設為 `1` | 少這個仍是模擬網 |
| `ADMIN_KEY` | 換一組 16 字元以上隨機字串 | 短於 16 會拒絕啟動 |
| `RISK_PCT` | `0.25` | 超過 1 會拒絕啟動 |
| `LEVERAGE` | `3` | 超過 5 會拒絕啟動 |
| `MAX_POSITIONS` | `3` | 先小，穩了再放 |
| `CG_API_KEY`、Telegram token | 順便換新 | 上次外洩事件的餘波 |

## 不用改任何檔案

上線狀態**全部**在 Zeabur 環境變數，資料夾裡沒有任何東西記錄它。
所以之後整包覆蓋更新不會把你退回模擬網。

| 變數 | 值 |
|---|---|
| `EXTRA_ARGS` | `--live` |
| `ALLOW_LIVE` | `1` |

兩個都要，少一個仍是模擬網。這是刻意的雙重保險。

萬一哪天真的被退回模擬網（例如環境變數被誤刪），伺服器啟動時會發現
「上次是正式網、這次是模擬網」，在日誌和 Telegram 大聲警告。

## 部署前

- 確認 Telegram 配對還在（提醒設定頁）。
- 帳本不用動：正式網與模擬網各自一份（`trader.live.json`、`trader.testnet.json`），
  績效永遠不會混在一起。

## 部署後第一天

- 啟動日誌要看到「交易　正式網（真實資金）」與「⚠ 正在對正式網下單」。
- 模擬單頁把 `每日最多開倉` 設 1、`分數門檻` 設 75。
- 第一筆成交後，到幣安 App 確認：部位在、停損／停利／移動停利三張單都在。
- 都對了才逐步放寬。

## 隨時想回模擬網

移除 `ALLOW_LIVE` 重新部署即可。金鑰可以留著，程式不會用它打正式網。

回模擬網時，正式網帳本原封不動。若正式網還有持倉，啟動時會提醒：
那些部位仍在幣安、停損單仍有效，只是模擬網模式不追蹤也不移損；
切回正式網會自動接續。想讓它們被管理，就別在有持倉時切換，
或先手動平倉再切。

## 績效不如預期時怎麼回頭

1. 正式網先把 `每日最多開倉` 設 0 或按「停止」，讓現有部位自然出場。
2. 全部平倉後移除 `ALLOW_LIVE`，回模擬網繼續累積樣本。
3. 想改參數就改，交易會自動歸入新的策略標籤，跟改前分開統計。
4. 模擬網新參數累積 40 筆以上、期望值為正，再重新上線。
   正式網的舊紀錄留在 `trader.live.json`，不會被覆蓋。

## 模擬網與正式網的實質差異

- 滑價：正式網小市值幣的市價停損可能滑 1–2%，-1R 實際會變 -1.3R 到 -1.6R。
- 流動性：第一週把流動性門檻拉高，只做市值前 100。
- 價格：模擬網的價格與現貨常常對不上，正式網不會有這個問題。
